{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Chrome: localhost にアタッチ",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5500",   // Live Server のURLに合わせる
      "webRoot": "${workspaceFolder}",
      "sourceMaps": true
    }
  ]
}
